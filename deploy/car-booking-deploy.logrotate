# Install: sudo cp deploy/car-booking-deploy.logrotate /etc/logrotate.d/car-booking-deploy
# Rotated weekly, keeps 8 weeks, compresses old logs.
/var/log/car-booking-deploy.log {
    weekly
    rotate 8
    compress
    delaycompress
    missingok
    notifempty
    create 0644 root root
}
